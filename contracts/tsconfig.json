{
  "compilerOptions": {
    "target": "es2020",
    "module": "commonjs",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "baseUrl": ".",
    "paths": {
      "typechain-types": ["../typechain-types"]
    },
    "types": [
      "hardhat",    // para reconocer import { ethers } from "hardhat"
      "hardhat-deploy",
      "mocha",      // para describe/it
      "chai",       // para expect
      "node"        // si usas globals de Node
    ]
  },
  "include": [
    "test/**/*.ts",
    "deploy/**/*.ts",
    "../typechain-types/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "artifacts",
    "cache",
    "scripts-legacy/**/*.ts", // al utilizar hardhat-deploy, /deploy es el directorio desde el cual hardhat toma los scripts de deploy; ya no desde /scripts, por eso lo nombramos legacy, y lo excluimos para que no moleste si tiene errores. Siempre podemos volver a incluirlo.
  ]
}